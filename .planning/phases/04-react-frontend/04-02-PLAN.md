---
phase: 04-react-frontend
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - client/src/App.jsx
  - client/src/pages/MyClassesPage.jsx
  - client/src/pages/KBBrowserPage.jsx
  - client/src/pages/SettingsPage.jsx
  - client/src/pages/LessonLibraryPage.jsx
  - client/src/pages/PrintPage.jsx
  - client/src/pages/AdminDashboardPage.jsx
  - client/src/pages/AdminTeachersPage.jsx
  - client/src/pages/AdminClassesPage.jsx
autonomous: true
requirements:
  - FE-07
  - FE-08
  - FE-09
  - FE-10

must_haves:
  truths:
    - "Teacher can view and edit their class roster with student cluster assignments"
    - "Teacher can browse lessons with search and view source files for editing"
    - "Teacher can browse knowledge bases and manage cluster-KB mappings"
    - "Teacher can configure LLM provider settings and save API keys"
    - "Teacher can print lesson plan versions from a dedicated print page"
    - "Admin can view institution-level overview, teacher list, and class distribution"
  artifacts:
    - path: "client/src/pages/MyClassesPage.jsx"
      provides: "Class roster with student cluster editing"
      contains: "my-classes"
    - path: "client/src/pages/LessonLibraryPage.jsx"
      provides: "Lesson listing, search, and AI source file editing"
      contains: "lesson-library"
    - path: "client/src/pages/KBBrowserPage.jsx"
      provides: "KB listing and cluster-KB mapping editor"
      contains: "kb-browser"
    - path: "client/src/pages/SettingsPage.jsx"
      provides: "LLM config form with save and test"
      contains: "settings"
    - path: "client/src/pages/PrintPage.jsx"
      provides: "Print preview with iframe and browser print()"
      contains: "print"
    - path: "client/src/pages/AdminDashboardPage.jsx"
      provides: "Admin institution overview"
      contains: "admin-dashboard"
  key_links:
    - from: "client/src/pages/MyClassesPage.jsx"
      to: "/api/teachers/:id/classes"
      via: "useApi().get"
      pattern: "api\\.get.*classes"
    - from: "client/src/pages/MyClassesPage.jsx"
      to: "/api/teachers/:id/students/:studentId"
      via: "useApi().patch"
      pattern: "api\\.patch.*students"
    - from: "client/src/pages/LessonLibraryPage.jsx"
      to: "/api/lessons"
      via: "useApi().get"
      pattern: "api\\.get.*lessons"
    - from: "client/src/pages/LessonLibraryPage.jsx"
      to: "/api/file-edits/lessons/:id/sources"
      via: "useApi().get"
      pattern: "api\\.get.*file-edits"
    - from: "client/src/pages/KBBrowserPage.jsx"
      to: "/api/clusters"
      via: "useApi().get"
      pattern: "api\\.get.*clusters"
    - from: "client/src/pages/KBBrowserPage.jsx"
      to: "/api/clusters/:id/kbs"
      via: "useApi().put"
      pattern: "api\\.put.*kbs"
    - from: "client/src/pages/SettingsPage.jsx"
      to: "/api/teachers/:id/llm-config"
      via: "useApi().get/.put"
      pattern: "api\\.(get|put).*llm-config"
    - from: "client/src/pages/PrintPage.jsx"
      to: "/api/adaptations/:id/versions/:vId"
      via: "useApi().get"
      pattern: "api\\.get.*versions"
---

<objective>
Build all data management pages: class roster with cluster editing, lesson library with source file editing, KB browser with cluster mapping, LLM settings, print preview, and admin overview. After this plan, teachers can manage all data through the React app and print lesson versions.

Purpose: Completes data CRUD functionality for teachers and admin overview — supports FE-07, FE-08, FE-09, and FE-10 (print portion).

Output: 8 new page components replacing placeholder "coming soon" divs in App.jsx.
</objective>

<execution_context>
@C:/Users/ethan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ethan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-react-frontend/04-CONTEXT.md
@.planning/phases/04-react-frontend/04-RESEARCH.md
@.planning/phases/04-react-frontend/04-PATTERNS.md
@.planning/phases/04-react-frontend/04-UI-SPEC.md
@.planning/phases/04-react-frontend/04-01-SUMMARY.md
@adapt-frontend-prototype-echristian-aduong/my-classes.html
@adapt-frontend-prototype-echristian-aduong/lesson-library.html
@adapt-frontend-prototype-echristian-aduong/kb-browser.html
@adapt-frontend-prototype-echristian-aduong/settings.html
@adapt-frontend-prototype-echristian-aduong/print.html
@adapt-frontend-prototype-echristian-aduong/admin-dashboard.html
@adapt-frontend-prototype-echristian-aduong/admin-teachers.html
@adapt-frontend-prototype-echristian-aduong/admin-classes.html
@server/src/routes/teachers.js
@server/src/routes/lessons.js
@server/src/routes/clusters.js
@server/src/routes/knowledge-bases.js
@server/src/routes/settings.js
@server/src/routes/admin.js
@server/src/routes/file-edits.js
@server/src/routes/adaptations.js

<interfaces>
<!-- API endpoints for data pages (from server/src/routes/) -->

From server/src/routes/teachers.js:
```
GET    /api/teachers/:id/classes    → Classes with nested students
PATCH  /api/teachers/:id/students/:studentId → Update student cluster_id
GET    /api/teachers/:id/dashboard → Dashboard data
```

From server/src/routes/lessons.js:
```
GET    /api/lessons                → { lessons, total, page, limit }
GET    /api/lessons/:id            → Single lesson
```

From server/src/routes/clusters.js:
```
GET    /api/clusters               → All clusters with kb_count, student_count
GET    /api/clusters/:id/kbs        → KBs for a cluster
PUT    /api/clusters/:id/kbs       → Replace cluster-KB mappings
```

From server/src/routes/knowledge-bases.js:
```
GET    /api/knowledge-bases         → All KBs
```

From server/src/routes/settings.js:
```
GET    /api/teachers/:id/llm-config → Current LLM config
PUT    /api/teachers/:id/llm-config  → Save LLM config
POST   /api/teachers/:id/llm-config/test → Test LLM connection (returns 501)
```

From server/src/routes/file-edits.js:
```
GET    /api/file-edits/lessons/:lesson_id/sources → Source files for a lesson
POST   /api/file-edits             → Create edited copy (returns edit result)
GET    /api/lesson-file-edits/:filename → Download edited file
```

From server/src/routes/adaptations.js:
```
GET    /api/adaptations/:adapted_id/versions/:version_id → Version detail (includes rendered_html)
GET    /api/adaptations/:adapted_id/versions/:version_id/print → Print HTML
```

From server/src/routes/admin.js:
```
GET    /api/institutions/:id/overview  → Admin dashboard data
GET    /api/institutions/:id/teachers  → Teachers list
GET    /api/institutions/:id/classes    → Classes list
```

From client/src/auth/AuthContext.jsx (from Plan 04-01):
```
export function useAuth() → { user: { teacherId, role, name, token }, login, logout }
export function useApi() → { get, post, put, patch, del }
export function toast(msg, kind) → void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MyClassesPage + KBBrowserPage + SettingsPage</name>
  <files>
    client/src/pages/MyClassesPage.jsx,
    client/src/pages/KBBrowserPage.jsx,
    client/src/pages/SettingsPage.jsx
  </files>
  <read_first>
    adapt-frontend-prototype-echristian-aduong/my-classes.html
    adapt-frontend-prototype-echristian-aduong/kb-browser.html
    adapt-frontend-prototype-echristian-aduong/settings.html
    client/src/api/useApi.js
    client/src/auth/AuthContext.jsx
    client/src/App.css
  </read_first>
  <action>
1. **Create `client/src/pages/MyClassesPage.jsx`** — port of `my-classes.html` per FE-07. Pattern: useState + useEffect + useApi (page-level data fetching per D-06, D-07).

   Data fetching: On mount, `Promise.all([api.get('/api/teachers/' + user.teacherId + '/classes'), api.get('/api/clusters')])` for parallel loading. Store in `[classes, setClasses]` and `[clusters, setClusters]` state.

   Render structure (port from my-classes.html):
   - Page header: `<div className="page-title">My Classes</div>` + `<div className="page-subtitle">{N} classes</div>`
   - Class panels: Each class has a card with class name, and inside it a `.student-edit-row` grid for each student showing avatar initials, name, cluster dropdown (populated from `clusters` data), and a "Save" button.
   - Cluster assignment edit: Select dropdown with cluster options (each cluster.id as value, cluster.name as label). On "Save" click: `api.patch('/api/teachers/' + user.teacherId + '/students/' + studentId, { cluster_id: newClusterId })`.
   - Saving pattern (per RESEARCH.md shared pattern): Disable button → show "Saving..." → call API → toast success → reload data → re-enable button.
   - Use toast from useApi.js for notifications.

2. **Create `client/src/pages/KBBrowserPage.jsx`** — port of `kb-browser.html` per FE-08. Pattern: useState + useEffect + useApi.

   Data fetching: On mount, `Promise.all([api.get('/api/knowledge-bases'), api.get('/api/clusters')])` for parallel loading.

   Render structure (port from kb-browser.html):
   - Page header: `<div className="page-title">Knowledge Bases</div>` + `<div className="page-subtitle">{N} knowledge bases</div>`
   - Two-column layout: `.kb-workspace` grid. Left column: KB listing with `.check-row` cards showing KB name, description. Right column (sidebar panel): Cluster-KB mapping editor.
   - Cluster selector: Dropdown to select a cluster. On change: `api.get('/api/clusters/' + clusterId + '/kbs')` to load current mappings.
   - KB toggle list: For the selected cluster, show KB items as `.mini-check` toggles (checked = assigned to cluster). Use `Set` for tracking selected KB IDs.
   - "Save KB Mapping" button: `api.put('/api/clusters/' + clusterId + '/kbs', { kb_ids: [...selectedKbIds] })`. Saving pattern: disable → "Saving..." → toast → reload clusters → re-enable.
   - Use toast from useApi.js.

3. **Create `client/src/pages/SettingsPage.jsx`** — port of `settings.html` per FE-09, RESEARCH Pitfall #4 (LLM test returns 501). Pattern: useState + useEffect + useApi.

   Data fetching: On mount, `api.get('/api/teachers/' + user.teacherId + '/llm-config')`. Store config in state.

   Render structure (port from settings.html):
   - Page header: `<div className="page-title">LLM Settings</div>` + `<div className="page-subtitle">Configure your AI provider</div>`
   - Form in a card (max-width 560px per UI-SPEC):
     - Provider dropdown: `<select>` with options (Gemini, OpenRouter, HuggingFace). Default from config.
     - Model input: `<input type="text">` for model name. Optional.
     - API key input: `<input type="password">` with hint text showing redacted current key (e.g., "Saved key: sk-...XXXX (paste new key to replace)").
     - "Save Configuration" button (`.btn-primary`). Calls `api.put('/api/teachers/' + user.teacherId + '/llm-config', { provider, model, api_key })`. Saving pattern.
     - "Test connection" button (`.btn`). Calls `api.post('/api/teachers/' + user.teacherId + '/llm-config/test')`. Per RESEARCH Pitfall #4: If response is 501, show toast "Test connection feature coming soon". Handle both success and error responses gracefully.
   - Help cards below form: Info about where to get API keys for each provider.
  </action>
  <verify>
    <automated>cd client && npm run build 2>&1 | Select-String -Pattern "error|fail" -CaseSensitive; if ($LASTEXITCODE -eq 0) { echo "BUILD OK" } else { echo "BUILD FAILED" }</automated>
  </verify>
  <done>
    - MyClassesPage fetches classes and clusters, displays student roster with cluster edit dropdowns
    - MyClassesPage PATCH calls `/api/teachers/:id/students/:studentId` on save with toast confirmation
    - KBBrowserPage fetches KBs and clusters, allows cluster-KB mapping with save
    - KBBrowserPage PUT calls `/api/clusters/:id/kbs` with selected KB IDs
    - SettingsPage fetches current LLM config and renders form with provider, model, API key
    - SettingsPage PUT calls `/api/teachers/:id/llm-config` on save
    - SettingsPage handles 501 from test endpoint with "coming soon" message
    - All three pages follow the page-level data fetching pattern (useState + useEffect + useApi)
  </done>
</task>

<task type="auto">
  <name>Task 2: LessonLibraryPage + PrintPage + Admin Pages</name>
  <files>
    client/src/pages/LessonLibraryPage.jsx,
    client/src/pages/PrintPage.jsx,
    client/src/pages/AdminDashboardPage.jsx,
    client/src/pages/AdminTeachersPage.jsx,
    client/src/pages/AdminClassesPage.jsx,
    client/src/App.jsx
  </files>
  <read_first>
    adapt-frontend-prototype-echristian-aduong/lesson-library.html
    adapt-frontend-prototype-echristian-aduong/print.html
    adapt-frontend-prototype-echristian-aduong/admin-dashboard.html
    adapt-frontend-prototype-echristian-aduong/admin-teachers.html
    adapt-frontend-prototype-echristian-aduong/admin-classes.html
    client/src/api/useApi.js
    client/src/auth/AuthContext.jsx
    client/src/App.jsx
    server/src/routes/file-edits.js
    server/src/routes/admin.js
  </read_first>
  <action>
1. **Create `client/src/pages/LessonLibraryPage.jsx`** — port of `lesson-library.html` per FE-08. This is a more complex page with source file editing.

   Data fetching: On mount, `api.get('/api/lessons?page=1&limit=50')` (or search). Store in `[lessons, setLessons]`.

   Render structure (port from lesson-library.html):
   - Page header: `<div className="page-title">Lesson Library</div>` + `<div className="page-subtitle">{N} lessons</div>`
   - Search input: Filter lessons by title (client-side filtering).
   - Two-column layout: `.library-workspace` grid. Left: lesson cards with title, subject badge (`.badge`), and description. Click a card to select it.
   - Right column (sidebar panel): Source file editor.
     - When a lesson is selected, `api.get('/api/file-edits/lessons/' + lessonId + '/sources')` to load source files.
     - Display source files list with file type badges.
     - "Edit with AI" form: textarea for edit instructions, file dropdown to select which source file to edit.
     - Submit: `api.post('/api/file-edits', { lesson_id: lessonId, source_filename: selectedFile, instructions: editInstructions })`.
     - Per PATTERNS.md Pitfall #1: Use `/api/file-edits/lessons/:id/sources` (NOT `/api/lessons/:id/source-files`).

   **File download with auth** (per RESEARCH Pitfall #7):
   - For downloading edited files: Use `api.get()` which includes Bearer token. If the response is a blob, create download link via `URL.createObjectURL(blob)`.
   - Alternative for direct download: Create an authenticated fetch with manually added `Authorization: Bearer` header.

2. **Create `client/src/pages/PrintPage.jsx`** — port of `print.html` per FE-10. Pattern: useParams/useSearchParams + useApi + iframe.

   This page receives query params: `adapted_id` and `version_id` (from workspace "Open Print View" link).

   Data fetching: On mount, fetch version data `api.get('/api/adaptations/' + adaptedId + '/versions/' + versionId)`. Get `rendered_html` from version response.

   Render structure (port from print.html):
   - Sticky toolbar at top with "Print or Save PDF" button (`.btn-primary`) and "Close" button.
   - Full-height sandboxed iframe: `<iframe ref={iframeRef} className="plan-frame" sandbox="allow-same-origin" title="Print preview" />`.
   - On data load: Set `iframeRef.current.srcDoc = version.rendered_html`.
   - Print button: `iframeRef.current.contentWindow.print()`.
   - Close button: `navigate(-1)` or `window.close()`.
   - `@media print` CSS rule to hide toolbar: In App.css, add `@media print { .print-toolbar { display: none; } }`.

3. **Create `client/src/pages/AdminDashboardPage.jsx`** — port of `admin-dashboard.html` per PATTERNS.md. Uses `GET /api/institutions/:id/overview`.

   Data fetching: On mount, `api.get('/api/institutions/' + user.institutionId + '/overview')`. Requires institution ID which comes from the dashboard API (user's `institution_id`).

   Render structure (port from admin-dashboard.html):
   - Page header: `<div className="page-title">Admin Overview</div>`
   - Metrics grid (4 columns): Teachers, Classes, Learner profiles, Adaptations.
   - Teachers table with columns: Teacher name, Email, Classes, Students, Adaptations. Use `.table-wrap > table` pattern.
   - Use `.metric` cards for stats, `.badge` for highlighting, `pickBadge` helper for row colors.

4. **Create `client/src/pages/AdminTeachersPage.jsx`** — uses `GET /api/institutions/:id/teachers`. Simpler page: list of teachers with email, role, active status. Table format with `.table-wrap > table`.

5. **Create `client/src/pages/AdminClassesPage.jsx`** — uses `GET /api/institutions/:id/classes`. List of classes with teacher name, student count, cluster distribution. Table format.

6. **Update `client/src/App.jsx`** to replace ALL remaining placeholder "coming soon" divs with the real page components. Import and use: MyClassesPage, KBBrowserPage, SettingsPage, LessonLibraryPage, PrintPage, AdminDashboardPage, AdminTeachersPage, AdminClassesPage. The updated routes:
   ```jsx
   <Route path="/my-classes" element={<MyClassesPage />} />
   <Route path="/lessons" element={<LessonLibraryPage />} />
   <Route path="/knowledge" element={<KBBrowserPage />} />
   <Route path="/settings" element={<SettingsPage />} />
   <Route path="/print" element={<PrintPage />} />
   <Route path="/admin" element={<AdminRoute />}>
     <Route index element={<AdminDashboardPage />} />
     <Route path="teachers" element={<AdminTeachersPage />} />
     <Route path="classes" element={<AdminClassesPage />} />
   </Route>
   ```
   Note: `/personalize` and `/workspace/:adaptedId` still have placeholders — those are Plan 04-03.
  </action>
  <verify>
    <automated>cd client && npm run build 2>&1 | Select-String -Pattern "error|fail" -CaseSensitive; if ($LASTEXITCODE -eq 0) { echo "BUILD OK" } else { echo "BUILD FAILED" }</automated>
  </verify>
  <done>
    - MyClassesPage shows class roster with editable student cluster assignments
    - KBBrowserPage shows KB listing and cluster-KB mapping editor
    - SettingsPage shows LLM config form with save and graceful 501 handling
    - LessonLibraryPage shows lesson listing with search and source file editing panel
    - LessonLibraryPage uses correct API path `/api/file-edits/lessons/:id/sources` (NOT `/api/lessons/:id/source-files`)
    - PrintPage renders lesson HTML in sandboxed iframe and provides print button
    - PrintPage uses `iframeRef.current.srcDoc` for content (per D-12)
    - AdminDashboardPage shows institution overview with metrics and teacher table
    - AdminTeachersPage shows teacher list for the institution
    - AdminClassesPage shows class list for the institution
    - All pages use useApi hook for data fetching with Bearer token auth
    - All admin pages are wrapped in AdminRoute in App.jsx
    - App.jsx has no remaining placeholder "coming soon" divs except for /personalize and /workspace
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Express API | Authenticated CRUD operations for classes, lessons, KBs, settings |
| Browser → File downloads | Authenticated file download requests for edited source files |
| Print iframe | Sandboxed content rendering isolated from app DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Spoofing | LessonLibraryPage file edit requests | mitigate | useApi adds Bearer token; server validates teacher ownership |
| T-04-07 | Tampering | KBBrowserPage cluster-KB mapping updates | mitigate | PUT request validated by server; teacher must own the cluster |
| T-04-08 | Info Disclosure | SettingsPage API key display | mitigate | API key shown as redacted value (`sk-...XXXX`); full key never displayed |
| T-04-09 | Elevation of Privilege | Admin pages access | mitigate | AdminRoute checks `user.role === 'admin'`; server requiresAdmin middleware |
| T-04-10 | Tampering | Print iframe injection | mitigate | Sandboxed iframe (`sandbox="allow-same-origin"`) per D-12; CSS/style isolation |
</threat_model>

<verification>
1. `cd client && npm run build` exits 0
2. `client/src/pages/MyClassesPage.jsx` contains `/api/teachers/` and `/students/` (PATCH cluster assignment)
3. `client/src/pages/KBBrowserPage.jsx` contains `/api/clusters` and `/kbs` endpoints
4. `client/src/pages/SettingsPage.jsx` contains `/llm-config` endpoint
5. `client/src/pages/LessonLibraryPage.jsx` contains `/file-edits/lessons/` (correct path, NOT `/lessons/:id/source-files`)
6. `client/src/pages/PrintPage.jsx` contains `sandbox="allow-same-origin"` and `srcDoc`
7. `client/src/pages/AdminDashboardPage.jsx` contains `/institutions/` endpoint
8. `client/src/App.jsx` has real component imports for all pages except /personalize and /workspace
</verification>

<success_criteria>
- All 8 data management pages render with real API data
- MyClassesPage allows student cluster assignment editing with save confirmation
- KBBrowserPage allows cluster-KB mapping with save confirmation
- SettingsPage configs LLM settings and handles 501 gracefully
- LessonLibraryPage uses correct file-edits API path (not prototype path)
- PrintPage renders iframe with sandbox attribute
- Admin pages show institution-level data
- All pages use useApi hook for authenticated API calls
- Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-react-frontend/04-02-SUMMARY.md`
</output>